// TODO: GET    /api/rooms           — list rooms
// TODO: POST   /api/rooms           — create room
// TODO: GET    /api/rooms/:id       — get room
// TODO: PATCH  /api/rooms/:id       — update room (name, language)
// TODO: DELETE /api/rooms/:id       — delete room
// TODO: POST   /api/rooms/:id/join  — join room
// TODO: POST   /api/rooms/:id/leave — leave room
